- @slide.errors.full_messages.each do |msg|
  = msg
= form_for(@slide) do |f|
  .field
    = f.label :title, 'title'
  .field
    = f.text_field :title
  .field
    = f.file_field :pdf
  .field
    = f.submit "Upload"
